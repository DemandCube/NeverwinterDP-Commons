apply plugin: 'maven-publish'

archivesBaseName = 'commons.yara.server'

eclipse {
  project {
    name = 'commons.yara.server'
  }
}

dependencies {
  compile project(':utils')
  compile project(':buffer')
  compile project(':netty')
  compile project(':cluster')
}
