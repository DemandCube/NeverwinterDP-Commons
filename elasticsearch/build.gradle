archivesBaseName = 'commons.elasticsearch'

eclipse {
  project {
    name = 'commons.elasticsearch'
  }
}

dependencies {
  compile project(':cluster')
  compile group: 'org.elasticsearch', name: 'elasticsearch', version: '1.2.2'
}

